import { BorderBlueDirective } from './border-blue.directive';

describe('BorderBlueDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderBlueDirective();
    expect(directive).toBeTruthy();
  });
});
